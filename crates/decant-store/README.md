# decant-store

SQLite persistence of decant run metrics: `record` a run, `summary` for the
`decant history` report. DB path: `$DECANT_DB_PATH`, else
`$XDG_DATA_HOME/decant/metrics.db`, else `~/.local/share/decant/metrics.db`.
